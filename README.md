# Fullstack ClojureScript pet project

* Compile project: ```clojure -m cljs.main -O simple -t node -d ./public -o ./public/server.js -c health-clj-project.server.index```
* Compile tests: ```clojure -m cljs.main  -t node -d ./tests -o ./tests/e2e/index.js -c tests.e2e.index```
